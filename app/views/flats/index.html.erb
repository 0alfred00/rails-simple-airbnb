<div class="row">
  <% @flats.each do |flat| %>
    <div class="col-md-4">
      <div class="card mb-4 box-shadow">
        <div class="card-body">
          <h5 class="card-title"><%= flat.name %></h5>
          <p class="card-text"><%= flat.description %></p>
          <div class="d-flex justify-content-between align-items-center">
            <small class="text-muted"><%= flat.address %></small>
            <span class="badge badge-primary badge-pill"><%= flat.price_per_night %> €</span>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
